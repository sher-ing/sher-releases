# Sher releases

This public repository contains Sher release artifacts, provenance records, and the workflows that publish and verify them. The application source remains in the private `sher-ing/sher` repository.

Install the latest stable release:

```sh
curl -fsSL https://sher.ing/install | sh
```

Every published release is immutable. Assets include SHA-256 checksums and GitHub artifact attestations.
